numpy
pillow
# optional, only for reassemble.py's --show live window:
# opencv-python
# optional, only for the spectrum.py diagnostic (talks to librtlsdr.dll
# directly via ctypes, so no pyrtlsdr):
# matplotlib
